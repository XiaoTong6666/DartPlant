name: dartplant_fixture
description: Dart AOT fixtures used to validate DartPlant metadata and ABI adapters.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  test: ">=1.25.7 <2.0.0"
